"""extract_engine - metadata-driven SQL Server extract engine.

See ../../SKILL.md for the skill overview and ../../references/ for detail on
each module. This package has no side effects on import: importing it touches
no database, no filesystem beyond the package itself, and makes no network
call.
"""
